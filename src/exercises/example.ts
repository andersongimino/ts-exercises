console.log(`Exercicio 1: Somar duas variáveis`);

function somar(a: number, b: number): number {
    return a + b;
}

const resultado = somar(5, 3);
console.log(`O resultado da soma é: ${resultado}`);
